import data from "$lib/content/data.json";

import type { Data } from "./lib.types";

export const { experience, experienceCategories, nav, projectCategories, projects, technologies } =
  data as Data;
